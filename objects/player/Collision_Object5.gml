
if (place_meeting(x, y, obj_bloco)) {
    // Colisão detectada, mova o boneco de volta
    // Pode ajustar a posição ou a lógica conforme necessário
    x = previous_x;
    y = previous_y;
}
/// @description Inserir descrição aqui
// Você pode escrever seu código neste editor
